- clean: ['~', '~/.config']

- shell:
  - [git submodule update --init --recursive, Installing submodules]


- link:
    ~/.shell:
      path: shell
      create: true
    ~/.config:
      path: config
      create: true
    ~/.completions:
      path: completions
      create: true

    ~/.ipython/profile_default/ipython_config.py: ipython/ipython_config.py
    ~/.ipython/profile_default/ipython_kernel_config.py: ipython/ipython_kernel_config.py

    ~/.bash_profile:
    ~/.bashrc:
    ~/.zshrc:
    ~/.tridactylrc:
    ~/.inputrc:

    ~/.scripts:
      path: scripts
      create: true

    /usr/local/share/zsh/site-functions/_watson: completions/watson-complete.sh
    /usr/local/share/zsh/site-functions/_gh: completions/gh-complete.sh

- create:
  - ~/projects
  - ~/github

- pip:
    file: python/requirements.txt
    binary: /usr/local/bin/pip3
    user: true
    stdout: true
    stderr: true

- pipx:
    file: python/requirements-pipx.txt
    stdout: true
    stderr: true

- shell:
  - [brew cleanup, Cleaning up]
